@page "/Main"
@using Core.Services.Movies.Models
@using Core.Services.Movies
@inject IMovieService MovieService
@if (movieModal != null)
{
    <MudGrid Justify="Justify.Center" Class="d-flex flex-xs-column flex-sm-row" Style="margin-top:20px">
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />
        <MovieIconCard Content="@movieModal" />

    </MudGrid>
    }
    @code {
private MovieModal movieModal;

    protected override async Task OnInitializedAsync()
    {
        movieModal = await MovieService.GetMovieDetails("tt0120737");
    }
    }
